<%= form_for @appointment do |f| %>

  <%= f.text_field(:zip, class: "home_field", placeholder: "Enter your Zip Code", maxlength: 5)%><br/>    
  <%= f.submit "Book Your Cleaning", class: 'get_started' %>
<% end %>